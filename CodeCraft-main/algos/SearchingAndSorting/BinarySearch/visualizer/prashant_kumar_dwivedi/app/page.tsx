// page.tsx is the entry point in Next.js App Router.
// It behaves like a top-level React component.
// It should simply render the main component.
import BinarySearch from "../BinarySearch";

export default function Page() {
  return <BinarySearch />;
}
